name: rsv_downstream_analysis
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - pandas
  - numpy
  - pysam
  - pyyaml
  - biopython
  - pip
  - pip:
      - fuc

#Updated version on the 14.11.2025 due to conflicts with previousely used python 3.7 during the env create